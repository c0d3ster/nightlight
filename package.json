{
  "name": "nightlight",
  "private": true,
  "description": "Runner for unattended Claude Code sessions against a repo's TASKS.md",
  "scripts": {
    "plan": "./plan.sh",
    "overnight": "./overnight.sh"
  }
}
