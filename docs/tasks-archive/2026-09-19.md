# Tasks archive — 2026-09-19

## #12 [stack: solo] Add turn/cache-read efficiency guidance to CLAUDE.md's Overnight Agent Workflow

PR: #28 (`overnight/2026-09-19/12-turn-efficiency-guidance`)

Added a "Turn efficiency" subsection to CLAUDE.md's Overnight Agent Workflow section documenting: batching independent read-only tool calls (`Read`/`Glob`/`Grep`) into a single turn; using `MultiEdit` instead of sequential `Edit` calls for 2+ edits to the same file; chaining verification commands (typecheck/lint/test) into one `Bash` invocation via `&&`; and preferring targeted grep/glob over full-file reads during investigation. Sourced from a cost audit of the 2026-08-06 c0d3ster session (200 turns, $16.61 for 2 tasks).
